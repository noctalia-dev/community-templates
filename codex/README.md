# Codex

![Codex CLI using the Noctalia theme](screenshot.png)

Tested with Codex CLI 0.149.0.

Select the generated syntax theme in `~/.codex/config.toml`:

```toml
[tui]
theme = "noctalia"
```

Codex applies syntax-theme changes in new sessions.
